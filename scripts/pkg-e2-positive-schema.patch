diff --git a/index.html b/index.html
index f793b1b..b529edf 100644
--- a/index.html
+++ b/index.html
@@ -2581,38 +2581,51 @@ $$('.tab').forEach(t=>t.onclick=()=>setRole(t.dataset.role));
 updateRoleFields($('#roleInput')?.value||'breeder');
 
 function getRows(key){try{return JSON.parse(localStorage.getItem(key)||'[]')}catch{return[]}}
-const MDZ_REGISTRATION_BACKUP_FIELDS=Object.freeze([
-  'registration_id','member_id','member_id_sequential','member_invite_code',
-  'first_name','last_name','full_name','birth_date','phone','email','whatsapp',
-  'wilaya','daira','commune','user_type','role','message','is_verified',
-  'privacy_accepted','founding_terms_accepted','invited_by','referral_code','bio',
-  'livestock_count','breeder_activity','vet_license','clinic_name','vet_specialty',
-  'vet_experience_years','graduation_year','coverage','vet_services','accept_requests',
-  'accept_qr','business_name','feed_types','delivery','buyer_interest','buyer_delivery',
-  'qualification','management_experience','manager_plan','ambassador_plan',
-  'partner_business','partnership_type','status','error','createdAt'
-]);
+const MDZ_REGISTRATION_BACKUP_SCHEMA=Object.freeze({
+  registration_id:'string',member_id:'string',member_id_sequential:'boolean',member_invite_code:'string',
+  first_name:'string',last_name:'string',full_name:'string',birth_date:'string',phone:'string',email:'string',whatsapp:'string',
+  wilaya:'string',daira:'string',commune:'string',user_type:'string',role:'string',is_verified:'boolean',
+  privacy_accepted:'boolean',founding_terms_accepted:'boolean',invited_by:'string',referral_code:'string',bio:'string',
+  livestock_count:'string',breeder_activity:'string',vet_license:'string',clinic_name:'string',vet_specialty:'string',
+  vet_experience_years:'string',graduation_year:'string',coverage:'string',vet_services:'string_array',accept_requests:'boolean',
+  accept_qr:'boolean',business_name:'string',feed_types:'string',delivery:'string',buyer_interest:'string',buyer_delivery:'string',
+  qualification:'string',management_experience:'string',manager_plan:'string',ambassador_plan:'string',
+  partner_business:'string',partnership_type:'string',status:'string',failure_code:'failure_code',createdAt:'string'
+});
 const MDZ_REGISTRATION_STORAGE_KEYS=Object.freeze(['mdz_registrations','mdz_failed_registrations']);
-const MDZ_SENSITIVE_STORAGE_FIELDS=new Set([
-  'password','password_confirm','access_token','refresh_token','authorization','token','secret'
-]);
+const MDZ_REGISTRATION_FAILURE_CODES=new Set(['auth_failed','network_failed','rate_limited','registration_failed']);
+function normalizeRegistrationBackupValue(type,value){
+  if(type==='boolean') return typeof value==='boolean'?value:undefined;
+  if(type==='string_array'){
+    if(!Array.isArray(value)) return undefined;
+    return value.filter(item=>typeof item==='string').slice(0,24).map(item=>item.slice(0,240));
+  }
+  if(type==='failure_code'){
+    const code=String(value||'');
+    return MDZ_REGISTRATION_FAILURE_CODES.has(code)?code:'registration_failed';
+  }
+  if(type==='string'){
+    if(typeof value!=='string'&&typeof value!=='number') return undefined;
+    return String(value).slice(0,2000);
+  }
+  return undefined;
+}
 function buildRegistrationBackup(...sources){
   const merged=Object.assign({},...sources.filter(Boolean));
   const backup={};
-  for(const key of MDZ_REGISTRATION_BACKUP_FIELDS){
-    if(Object.prototype.hasOwnProperty.call(merged,key)) backup[key]=merged[key];
+  for(const [key,type] of Object.entries(MDZ_REGISTRATION_BACKUP_SCHEMA)){
+    if(!Object.prototype.hasOwnProperty.call(merged,key)) continue;
+    const value=normalizeRegistrationBackupValue(type,merged[key]);
+    if(value!==undefined) backup[key]=value;
   }
   return backup;
 }
-function stripSensitiveRegistrationFields(value){
-  if(Array.isArray(value)) return value.map(stripSensitiveRegistrationFields);
-  if(!value||typeof value!=='object') return value;
-  const clean={};
-  for(const [key,item] of Object.entries(value)){
-    if(MDZ_SENSITIVE_STORAGE_FIELDS.has(String(key).toLowerCase())) continue;
-    clean[key]=stripSensitiveRegistrationFields(item);
-  }
-  return clean;
+function normalizeRegistrationBackupRows(value){
+  if(!Array.isArray(value)) return [];
+  return value
+    .filter(row=>row&&typeof row==='object'&&!Array.isArray(row))
+    .map(row=>buildRegistrationBackup(row))
+    .filter(row=>Object.keys(row).length>0);
 }
 function scrubRegistrationBackupStorage(storage=localStorage){
   for(const key of MDZ_REGISTRATION_STORAGE_KEYS){
@@ -2620,19 +2633,21 @@ function scrubRegistrationBackupStorage(storage=localStorage){
       const rawValue=storage.getItem(key);
       if(!rawValue) continue;
       const parsed=JSON.parse(rawValue);
-      const cleaned=stripSensitiveRegistrationFields(parsed);
+      const cleaned=normalizeRegistrationBackupRows(parsed);
       const encoded=JSON.stringify(cleaned);
       if(encoded!==rawValue) storage.setItem(key,encoded);
     }catch(error){
-      console.warn(`Credential scrub skipped for ${key}:`,error);
+      storage.setItem(key,'[]');
+      console.warn(`Malformed registration backup reset for ${key}`);
     }
   }
 }
 function saveRow(key,row){
   try{
-    const rows=getRows(key);
+    const registrationBackup=MDZ_REGISTRATION_STORAGE_KEYS.includes(key);
+    const rows=registrationBackup?normalizeRegistrationBackupRows(getRows(key)):getRows(key);
     const safeRow=MDZ_REGISTRATION_STORAGE_KEYS.includes(key)
-      ? stripSensitiveRegistrationFields(row)
+      ? buildRegistrationBackup(row)
       : row;
     rows.push({...safeRow,createdAt:new Date().toISOString()});
     // نحتفظ بآخر 100 سجل فقط حتى لا يمتلئ تخزين الهاتف ويُظهر خطأً كاذبًا بعد نجاح التسجيل.
@@ -2645,6 +2660,15 @@ function saveRow(key,row){
 }
 scrubRegistrationBackupStorage();
 
+function registrationFailureCode(error){
+  const status=Number(error?.status||error?.statusCode||0);
+  const code=String(error?.code||error?.name||'').toLowerCase();
+  if(status===429||code.includes('rate')) return 'rate_limited';
+  if(status===400||status===401||status===403||code.includes('auth')) return 'auth_failed';
+  if(code==='aborterror'||code==='typeerror'||code.includes('network')) return 'network_failed';
+  return 'registration_failed';
+}
+
 
 function makeRegistrationId(){
   const d=new Date();
@@ -2837,6 +2861,7 @@ $('#registerForm').addEventListener('submit', async e=>{
       emailData:{...payload,...ids,registration_id:ids.registrationId,member_id:ids.memberId,role_label:registrationRoleLabel(raw.role),member_invite_code:ids.memberInviteCode,invited_by:extra.invited_by}
     });
     if(!pipelineResult.success){
+      saveRow('mdz_failed_registrations',buildRegistrationBackup(raw,extra,{failure_code:registrationFailureCode(pipelineResult.error)}));
       showRegistrationError(confirmBox,pipelineResult.error?.message||t('authErrGeneric'));
       restoreSubmit();
       return;
@@ -2852,7 +2877,7 @@ $('#registerForm').addEventListener('submit', async e=>{
     updateAuthChrome();
   }catch(error){
     console.error('Registration pipeline error',error);
-    saveRow('mdz_failed_registrations',buildRegistrationBackup(raw,extra,{error:String(error)}));
+    saveRow('mdz_failed_registrations',buildRegistrationBackup(raw,extra,{failure_code:registrationFailureCode(error)}));
     showToast(t('authErrGeneric'));
     restoreSubmit();
   }finally{restoreSubmit()}
@@ -4139,4 +4164,4 @@ window.addEventListener('DOMContentLoaded',async()=>{
 </nav>
 
 </body>
-</html>
\ No newline at end of file
+</html>
