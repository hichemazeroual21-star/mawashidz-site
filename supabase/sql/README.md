# Supabase SQL — Phase 0 Final

**Canonical source:** `../migrations/20260720_phase0_final.sql`

`phase0_core.sql` is a human-readable extract of the core functions only. It is **not** executed independently. All deployments must run the final migration.

Archived migrations live in `../migrations/archive/`.
